# Local development override
# This file is automatically loaded by docker-compose
# For local dev: creates the nginx-proxy network locally
# In production: ansible creates the network before deploying

services:
  web:
    ports:
      - "8000:80"

networks:
  nginx-proxy:
    external: false
    driver: bridge
